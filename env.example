# Pixscaler Environment Configuration
# Copy this file to .env and set your own values

# ===== REQUIRED FOR PRODUCTION =====
# These MUST be set before deploying to production
# Generate secure random strings for these values using:
# - Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# - OpenSSL: openssl rand -hex 32

JWT_SECRET=CHANGE_ME_IN_PRODUCTION_REQUIRED
SESSION_SECRET=CHANGE_ME_IN_PRODUCTION_REQUIRED
NODE_ENV=production
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ===== SERVER CONFIGURATION =====
PORT=3000
HOST=localhost

# ===== DATABASE CONFIGURATION =====
DB_PATH=./database/pixscaler.db

# ===== SECURITY CONFIGURATION =====
# Enable/disable rate limiting (set to 'false' for production)
DISABLE_RATE_LIMITING=false

# Rate limiting thresholds
RATE_LIMIT_FREE_IMAGES_PER_HOUR=10
RATE_LIMIT_PREMIUM_IMAGES_PER_HOUR=1000
RATE_LIMIT_API_REQUESTS_PER_15MIN=100
RATE_LIMIT_AUTH_ATTEMPTS_PER_15MIN=5

# ===== FILE UPLOAD CONFIGURATION =====
MAX_FILE_SIZE_FREE_MB=5
MAX_FILE_SIZE_PREMIUM_MB=50

# ===== OAUTH PROVIDERS (OPTIONAL) =====
# For social login - leave empty if not using
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ===== EMAIL CONFIGURATION (OPTIONAL) =====
# For email verification and notifications
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=noreply@pixscaler.com

# ===== PAYMENT CONFIGURATION (OPTIONAL) =====
# For Stripe subscription payments
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# ===== ANALYTICS CONFIGURATION (OPTIONAL) =====
# For Google Analytics and AdSense
GOOGLE_ANALYTICS_ID=GA_MEASUREMENT_ID
GOOGLE_ADSENSE_ID=ca-pub-...

# ===== SUBSCRIPTION PLANS CONFIGURATION =====
BASIC_PLAN_PRICE=3.99
SMART_PLAN_PRICE=9.99
PROFESSIONAL_PLAN_PRICE=19.99

# ===== IMAGE PROCESSING CONFIGURATION =====
SHARP_CACHE_SIZE_MB=50
SHARP_CONCURRENCY=4

# ===== LOGGING CONFIGURATION =====
LOG_LEVEL=info
LOG_FILE=./logs/pixscaler.log
DEBUG_MODE=false

# ===== DEVELOPMENT ONLY =====
# These are only used in development
ENABLE_API_DOCS=false

# ===== PRODUCTION DEPLOYMENT CHECKLIST =====
# Before deploying to production, ensure:
# 
# ✅ JWT_SECRET is set to a secure random string (minimum 32 characters)
# ✅ SESSION_SECRET is set to a secure random string (minimum 32 characters)  
# ✅ NODE_ENV is set to 'production'
# ✅ CORS_ORIGINS is set to your actual domain(s) 
# ✅ DATABASE_PATH points to a secure location
# ✅ All sensitive keys are stored securely (not in version control)
# ✅ Email configuration is set up if using notifications
# ✅ Stripe keys are set if using payments
# ✅ Analytics IDs are configured
# ✅ Rate limiting is enabled (DISABLE_RATE_LIMITING=false)
# ✅ File size limits are appropriate for your hosting
# ✅ HTTPS is enabled on your hosting platform
# ✅ Database backups are configured
#
# Security Tips:
# - Never commit .env files to version control
# - Use strong, unique secrets for each environment
# - Regularly rotate secrets and API keys
# - Monitor logs for suspicious activity
# - Keep dependencies updated 